# This module's AEL usage relies on server-side AEL compilation, added to the SDK after the
# 0.9.0a5 public PyPI release - see README.md's "Expressions: AEL" section. Until a public
# release including that fix ships, this pins an Aerospike-internal dev build; installing it
# requires credentials for Aerospike's internal package index (README.md's Setup section has
# the details) and is not possible for external contributors. Once a public release with the
# fix is available, this should switch back to a plain public-PyPI pin.
aerospike-sdk==0.9.0a6.dev95
